/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { StatuspengujianService } from './statuspengujian.service';

describe('Service: Statuspengujian', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [StatuspengujianService]
    });
  });

  it('should ...', inject([StatuspengujianService], (service: StatuspengujianService) => {
    expect(service).toBeTruthy();
  }));
});
